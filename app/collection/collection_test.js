'use strict';

describe('pesto.collection module', function() {

  beforeEach(module('pesto.collection'));

  describe('pesto controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('CollectionCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});